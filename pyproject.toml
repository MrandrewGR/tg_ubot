# tg_ubot/pyproject.toml
[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tg_ubot"
version = "0.1.0"
python-requires = ">=3.7"
dependencies = [
  "telethon==1.31.0",
  "aiokafka==0.12.0",
  # и т.д.
]
